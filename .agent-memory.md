# Agent Memory

## 2026-05-08
- Preference: Prioritize broad VS Code extension API compatibility in `src/extension-host/vscode-shim.js` and `src/extension-host/vscode-shim.cjs` when adding extension support features.
- Friction observed: Missing API symbols in shim can block third-party extensions at runtime even when core host wiring exists.
- Preventive action: Mirror API additions in both shim variants and verify with syntax checks + full build.
