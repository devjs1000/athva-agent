# Routes

- Chat workflow entrypoints live in `src/modules/chatbot.ts` and `src/utils/chatbot.ts`.
- Explorer-driven folder navigation enters through `src/modules/file-explorer.ts` and is wired in `src/main.ts`.
- Context workspace navigation resolves through `src/modules/contexts-workspace.ts`.
